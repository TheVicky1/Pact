"use server";
import { redirect } from "next/navigation";
import { z } from "zod";
import { requireUser } from "@/features/auth/require-user";
const schema = z.object({ displayName: z.string().trim().min(2).max(80), timezone: z.string().trim().min(1).max(64), currencyCode: z.literal("INR"), intention: z.enum(["consistency", "study", "spending", "habits", "everything"]) });
export async function completeOnboarding(formData: FormData) { const parsed = schema.safeParse(Object.fromEntries(formData)); if (!parsed.success) throw new Error("Please complete the required onboarding fields."); const { supabase, userId } = await requireUser(); const { data: profile, error: readError } = await supabase.from("profiles").select("preferences").eq("id", userId).single(); if (readError) throw new Error("We couldn't load your profile. Please try again."); const { error } = await supabase.from("profiles").update({ display_name: parsed.data.displayName, timezone: parsed.data.timezone, currency_code: parsed.data.currencyCode, preferences: { ...(profile.preferences ?? {}), primary_intention: parsed.data.intention }, onboarding_completed_at: new Date().toISOString() }).eq("id", userId); if (error) throw new Error("We couldn't save your onboarding choices. Please try again."); redirect("/app"); }
